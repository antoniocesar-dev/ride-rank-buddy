# docker-compose.yml — Ride Rank Buddy production stack
# Pure frontend SPA (Vite + React + Supabase). No backend container.
# Routing handled by platform-level Traefik (platform_public network).
# Served at /ranking (IP-only) or https://<DOMAIN>/ranking (domain mode).
#
# IP-only mode:
#   docker compose -f docker-compose.yml -f docker-compose.vps.yml up -d
#
# Domain mode (after DNS is live):
#   DOMAIN=riderank.grupolamonica.com.br \
#   docker compose -f docker-compose.yml -f docker-compose.vps.yml -f docker-compose.domain.yml up -d

services:

  # ── Frontend (nginx:alpine serving Vite dist) ─────────────────────────────
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_SUPABASE_URL: ""
        VITE_SUPABASE_PUBLISHABLE_KEY: ""
    image: riderank-frontend:latest
    restart: unless-stopped
    networks:
      - riderank-net
      - platform_public
    healthcheck:
      test: ["CMD", "curl", "-fs", "http://localhost:80/"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=platform_public"
      # PathPrefix /ranking — higher priority than Cargas catch-all (priority 1)
      - "traefik.http.routers.rr-frontend.rule=PathPrefix(`/ranking`)"
      - "traefik.http.routers.rr-frontend.entrypoints=web"
      - "traefik.http.routers.rr-frontend.priority=5"
      - "traefik.http.routers.rr-frontend.middlewares=rr-stripprefix"
      - "traefik.http.services.rr-frontend.loadbalancer.server.port=80"
      # Strip /ranking before forwarding to nginx (nginx stays path-agnostic)
      - "traefik.http.middlewares.rr-stripprefix.stripprefix.prefixes=/ranking"

# ── Networks ──────────────────────────────────────────────────────────────
networks:
  riderank-net:
    name: riderank-net
    driver: bridge
  platform_public:
    external: true
    name: platform_public
